#!/usr/bin/env python3

import re
import sys

path, benchmark = sys.argv[1:]

with open(path) as f:
    for line in f:
        line = line.rstrip()
        line = re.sub(r'\$\(benchmark\)', benchmark, line)
        print(line)
